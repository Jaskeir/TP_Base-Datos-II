CREATE VIEW VW_DUEÑOS AS
 SELECT
 c.DNICLIENTE AS DNI,c.Nombre_Cliente AS Nombre, c.Direccion_Cliente AS Direccion,t.Telefono_Cliente As Telefono,t.Observaciones,m.Nombre_Mascota,m.Especie
FROM CLIENTES c 
INNER JOIN 
MASCOTAS a ON c.Od_Cliente = m.d_Cliente;
INNER JOIN 
TELEFONOS_CLIENTES a ON m.Codigo_Mascota= t.Codigo_Mascota;